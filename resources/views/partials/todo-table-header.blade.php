<thead>
  <tr class="todo-table-header">
    <th class="todo-col-check" scope="col">{{ __('対象') }}</th>
    <th class="todo-col-date" scope="col">{{ __('日付') }}</th>
    <th class="todo-col-title" scope="col">{{ __('内容') }}</th>
    <th class="todo-col-time" scope="col">{{ __('時間帯') }}</th>
    <th class="todo-col-category" scope="col">{{ __('ステータス') }}</th>
    <th class="todo-col-importance" scope="col">{{ __('重要度') }}</th>
    <th class="todo-col-actions" scope="col">{{ __('操作') }}</th>
  </tr>
</thead>
