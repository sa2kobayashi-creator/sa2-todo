# Android APK（ダッシュボードのインストールリンク）

TWA ビルドで得た `app-release-signed.apk` をこのフォルダに  
`sa2-plus.apk` という名前で置いてください（`twa/README.md` 参照）。

本番では `public/downloads/sa2-plus.apk` をデプロイするか、  
`.env` の `ANDROID_APK_URL` に外部配布URLを設定します。
